import React, { useState } from "react";

export default function Navbar() {
  const [imgOk, setImgOk] = useState(true);

  return (
    <>
      <nav
        id="topbar"
        className="w-full sticky top-0 z-50 bg-gradient-to-r from-[#0a1a5f] via-[#3db8ff] via-[#1e6fff] via-[#8ecaff] via-[#6a00ff] to-[#8a3dff] text-white shadow-md py-3"
      >
        <div className="mx-auto flex items-center justify-between px-4 md:px-6" style={{ maxWidth: 1200 }}>
          <a href="/" className="flex items-center">
            {imgOk ? (
              <img
                src="/logo.png"
                alt="Logo OMT"
                className="h-8 w-auto block"
                onError={() => setImgOk(false)}
              />
            ) : (
              <span style={{ fontWeight: 800, color: "#0EA5E9", fontSize: 20, marginLeft: 6 }}>OMT</span>
            )}
          </a>

          <a
            href="#login"
            className="bg-white/20 hover:bg-white/30 text-white font-semibold px-4 py-2 rounded-lg border border-white/30 transition-colors"
          >
            Para quem já é cadastrado Acessar/Login
          </a>
        </div>
      </nav>

      <div style={{ height: 56 }} aria-hidden />
    </>
  );
}